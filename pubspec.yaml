name: education_for_all

description: A new Flutter project for the Education for All App

# Flutter

# The following section is specific to Flutter.
# For more information on dependecies, see:
# https://flutter.dev/dependencies

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true
  # Add your project-specific assets here:
  assets:
    - assets/images/
    - assets/icons/
